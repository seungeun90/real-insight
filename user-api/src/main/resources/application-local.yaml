server:
  port: 8082
spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: user
      username: admin
      password: secret
      authentication-database: admin
  rabbitmq:
    host: localhost
    port: 5672
    username : admin
    password: secret


logging:
  level:
    org.springframework.data.mongodb.core.MongoTemplate: DEBUG