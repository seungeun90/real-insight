server:
  port: 8082
spring:
  application:
    name: user-api
  data:
    mongodb:
      host: ec2-3-38-240-66.ap-northeast-2.compute.amazonaws.com
      port: 27017
      database: user
      username: admin
      password: secret
      authentication-database: admin
  rabbitmq:
    host: 15.164.133.223
    port: 5672
    username : admin
    password: secret
logging:
  level:
    org.springframework.data.mongodb.core.MongoTemplate: DEBUG
management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  metrics:
    tags:
      application: ${spring.application.name}
    export:
      prometheus:
        enabled: true
